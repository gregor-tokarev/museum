<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/style/header.css">
    <link rel="stylesheet" href="/static/style/search.css">
    <link rel="stylesheet" href="/static/style/toys.css">
    <link rel="stylesheet" href="/static/style/main.css">
    <title>Список игрушек</title>
</head>

<body>
    <%- include('blocks/header.ejs')%>
        <%- include('blocks/search_b.ejs')%>
            <div class="toys_block container">
                <%toys.forEach(toy => {%>
                    <div class="toy">
                        <div class="toy_ph">
                            <%if(toy.images.length) {%>
                                <img src="<%= toy.images[0]%>" alt="">
                            <%}%>
                        </div>
                        <div class="toy_body">
                            <div class="toy_rarity"><%= toy.status%></div>
                            <div class="toy_name"><%= toy.title%></div>
                            <div class="toy_descr">
                                <%= toy.desc%>
                            </div>
                            <a class="toy_button" href="/toy/<%= toy._id%>">Смотреть</a>
                        </div>
                    </div>
                    <%})%>
            </div>
</body>

</html> 